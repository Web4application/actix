RUST_LOG=info
SERVER_HOST=102.89.23.201
SERVER_PORT=8080
nameseevers=locallhost.me
nameseevers=localhost.me
nameservers=fca2a8c6bfb418bc.vercel-dns-017.com.

export SERVER_PORT=8080 - Linux/Mac
set SERVER_PORT=8081 - Windows
.env file configuration
Docker environment variables
Kubernetes ConfigMaps

GODEBUG=fips140=only
GOFIPS140=v1.0.0
OPENSSL_CONF=/usr/lib/ssl/openssl.cnf
OPENSSL_MODULES=/usr/lib/aarch64-linux-gnu/ossl-modules
OPENSSL_VERSION=3.5.4
SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt

# Backend application.yml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myapp
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

# Frontend .env
REACT_APP_API_URL=http://localhost:8080
