# --- SERVER CONFIG ---
RUST_LOG=info
SERVER_HOST=102.89.23.201
SERVER_PORT=8080

# Pick ONE valid nameserver (you had duplicates)
NAMESERVER=localhost.me
NAMESERVER2=fca2a8c6bfb418bc.vercel-dns-017.com.

# --- DATABASE CONFIG ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=myapp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# --- FRONTEND CONFIG ---
FRONTEND_API_URL=http://localhost:8080

# --- SECURITY / OPENSSL / FIPS ---
GODEBUG=fips140=only
GOFIPS140=v1.0.0

OPENSSL_CONF=/usr/lib/ssl/openssl.cnf
OPENSSL_MODULES=/usr/lib/aarch64-linux-gnu/ossl-modules
OPENSSL_VERSION=3.5.4
SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
